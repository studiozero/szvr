aside.col-md-4
    h2 On the blog
    .row
        - var n = 0;
        - var p = 0;
        while p < 3 && n < _site_data.blog.length
            - var post = _site_data.blog[n]
            - var test = (post._metadata.title !== _metadata.title)

            if test
                .post.col-xs-6.col-sm-4.col-md-12
                    include _partials/_blog_card.pug
                - n++
                - p++
            else
                - n++