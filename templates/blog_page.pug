html
    head
        title #{attributes.title}
        <meta name="description">#{attributes.description}</meta>
    body
        h1 #{attributes.title}
        .main(data-author!=`${attributes.author}`) !{rendered}
        include footer.pug